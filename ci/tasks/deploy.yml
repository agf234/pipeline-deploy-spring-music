---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: agf234/pcfdev-cli
    tag: latest

run:
  path: pipeline-repo/ci/scripts/deploy.sh

inputs:
- name: pipeline-repo
- name: binary
